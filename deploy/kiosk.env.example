# Copy to kiosk.env (gitignored) and fill in. Loaded by rack-kiosk.service.
# Home Assistant MQTT control (auto-discovery). Leave MQTT_HOST unset to disable.
MQTT_HOST=10.0.0.94
MQTT_PORT=1883
MQTT_USER=user
MQTT_PASS=passwd

# Display defaults
KIOSK_DWELL=7
KIOSK_BRIGHTNESS=70
